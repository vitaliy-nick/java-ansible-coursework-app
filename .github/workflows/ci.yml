name: CI/CD Java (Maven) with Ansible

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      JAVA_VERSION: '17'
      APP_JAR_NAME: 'java-app-0.0.1-SNAPSHOT.jar'
      TARGET_HOST: '192.168.49.1'
      TARGET_USER: 'ubuntu'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'
          cache: 'maven'

      - name: Build with Maven (CI Stage: create JAR)
        run: mvn -B package --file pom.xml -DskipTests

      - name: Install Ansible
        run: pip install ansible

      - name: Configure SSH private key (для доступу до VM)
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Run Ansible Deploy (CD Stage: розгортання)
        run: |
          # 1. Створення інвенторі-файлу динамічно
          echo "[web]" > inventory
          echo "${{ env.TARGET_HOST }} ansible_user=${{ env.TARGET_USER }}" >> inventory

          # 2. Копіювання Playbook, шаблонів та АРТЕФАКТУ у робочий каталог
          mkdir ansible_temp
          
          # Копіювання файлів конфігурації Ansible
          cp -r ansible/templates ./ansible_temp/templates
          cp ansible/deploy.yml ./ansible_temp/deploy.yml

          # !!! ВИПРАВЛЕНО !!!: Копіювання JAR-файлу в робочий простір Ansible
          # Файл береться з target/ і кладеться в ansible_temp/
          cp target/${{ env.APP_JAR_NAME }} ansible_temp/${{ env.APP_JAR_NAME }}

          # 3. Запуск Playbook з передачею назви JAR-файлу як змінної
          # ПЕРЕХІД В КАТАЛОГ ДЕ ЛЕЖИТЬ PLAYBOOK
          cd ansible_temp
          
          ansible-playbook \
          -i ../inventory \
          deploy.yml \
          --extra-vars "app_jar_name=${{ env.APP_JAR_NAME }}"
